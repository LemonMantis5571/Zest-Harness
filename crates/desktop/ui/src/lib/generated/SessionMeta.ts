// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ModelCapability } from "./ModelCapability";
import type { PendingInputView } from "./PendingInput";
import type { ThreadCheckpointView } from "./ThreadCheckpoint";
import type { TurnRecoveryView } from "./TurnRecovery";

/**
 * A session without its transcript, for operations that do not change one.
 *
 * Deliberately a sibling of [`SessionInfo`] rather than a field inside it:
 * `#[serde(flatten)]` and `ts-rs` do not agree about how to describe that, and
 * the wire shape is what the generated bindings gate protects. The duplicated
 * field list is the cost of both staying legible.
 */
export type SessionMeta = { sessionId: string, provider: string, label: string, model: string, effort: string, root: string, isFreeChat: boolean, threadId: string, defaultModel: string, 
/**
 * See [`SessionInfo::owns_agent_loop`]. Present on both so the UI's
 * `{ ...meta }` merge after a model change cannot drop it.
 */
ownsAgentLoop: boolean, models: Array<ModelCapability>, checkpoints: Array<ThreadCheckpointView>, pendingInputs: Array<PendingInputView>, warning?: string, recovery?: TurnRecoveryView, };
