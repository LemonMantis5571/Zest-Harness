// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ToolMetaView } from "./ToolMetaView";
import type { WorkspaceChange } from "./WorkspaceChange";

export type ChatEvent = { "kind": "user", session_id: string, thread_id: string, turn_id: string, message_id: string, text: string, } | { "kind": "assistant_start", session_id: string, thread_id: string, turn_id: string, message_id: string,
/**
 * Slash command that produced this turn, when one did. The UI titles
 * the answer with it — Rust decides, because only Rust knows whether
 * a leading `/token` matched a real skill.
 */
command?: string | null, } | { "kind": "text_delta", session_id: string, thread_id: string, turn_id: string, message_id: string, text: string, } | { "kind": "thinking_delta", session_id: string, thread_id: string, turn_id: string, message_id: string, text: string, } | { "kind": "provider_activity", session_id: string, thread_id: string, turn_id: string, message_id: string, id: string, title: string, status: string, } | { "kind": "tool_call_start", session_id: string, thread_id: string, turn_id: string, message_id: string, name: string, id: string, } | { "kind": "tool_call_update", session_id: string, thread_id: string, turn_id: string, message_id: string, id: string, metadata: ToolMetaView, } | { "kind": "tool_call_result", session_id: string, thread_id: string, turn_id: string, message_id: string, name: string, id: string, summary: string, isError: boolean, path?: string, diff?: string, metadata?: ToolMetaView, } | { "kind": "approval_needed", session_id: string, thread_id: string, turn_id: string, message_id: string, approval_id: string, tool_name: string, tool_call_id: string, risk: string, path: string, summary: string, diff: string, } | { "kind": "question_needed", session_id: string, thread_id: string, turn_id: string, message_id: string, question_id: string, tool_call_id: string, prompt: string, choices: Array<string>, multiple: boolean, placeholder?: string, } | { "kind": "workspace_changed", session_id: string, thread_id: string, turn_id: string, change: WorkspaceChange, } | { "kind": "done", session_id: string, thread_id: string, turn_id: string, message_id: string, } | { "kind": "error", session_id: string, thread_id: string, turn_id: string, message_id: string, message: string,
/**
 * Provider to offer a Reconnect for, when the failure is one that only
 * signing in again can fix. `None` for everything else — a Reconnect
 * button on a rate limit would send the user through OAuth for nothing.
 */
reconnect_provider?: string | null,
/**
 * Provider whose failed API key or CLI-owned sign-in must be repaired
 * manually, or replaced by choosing another provider. This remains
 * separate from `reconnect_provider` so the UI never launches a
 * managed login flow for credentials Zest does not own.
 */
provider_selection?: string, } | { "kind": "cancelled", session_id: string, thread_id: string, turn_id: string, message_id: string, } | { "kind": "warning", session_id: string, thread_id: string, turn_id?: string, message: string, };
